version: '3.8'

services:
  web:
    build: .
    command: python /dj_psql_docker/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/dj_psql_docker
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:12
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"